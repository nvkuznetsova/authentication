doctype html
html
    head
        meta(charset='utf-8')
        title= title
    body
        h4= title
        each item in items
            p= 'User login: ' + item.login + ' User password: ' + item.password

        a(href='/logout') logout